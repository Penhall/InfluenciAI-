<Window
    x:Class="InfluenciAI.Desktop.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Login"
    Width="380"
    Height="240"
    WindowStartupLocation="CenterScreen"
    >
    <Grid Margin="20" Background="{StaticResource PrimaryBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Foreground="{StaticResource PrimaryTextBrush}" Text="E-mail" />
        <TextBox Grid.Row="1" />

        <TextBlock
            Grid.Row="2"
            Margin="0,10,0,0"
            Foreground="{StaticResource PrimaryTextBrush}"
            Text="Senha"
            />
        <PasswordBox x:Name="PwdBox" Grid.Row="3" />

        <StackPanel
            Grid.Row="3"
            Margin="0,32,0,0"
            Orientation="Horizontal"
            >
            <Button
                Width="100"
                Height="73"
                Click="Button_Click"
                Command="{Binding LoginCommand}"
                Content="Entrar"
                />
            <Button
                Width="100"
                Height="71"
                Margin="10,0,0,0"
                Click="Exit_Click"
                Content="Sair"
                />
            <TextBlock
                Margin="10,5,0,0"
                Foreground="{StaticResource ErrorColor}"
                Text="{Binding Status}"
                />
        </StackPanel>
    </Grid>
</Window>
