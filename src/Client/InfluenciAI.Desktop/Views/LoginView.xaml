<Window x:Class="InfluenciAI.Desktop.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="240" Width="380"
        WindowStartupLocation="CenterScreen">
    <Grid Background="{StaticResource PrimaryBackgroundBrush}" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="E-mail" Foreground="{StaticResource PrimaryTextBrush}"/>
        <TextBox Grid.Row="1"/>

        <TextBlock Grid.Row="2" Text="Senha" Margin="0,10,0,0" Foreground="{StaticResource PrimaryTextBrush}"/>
        <PasswordBox Grid.Row="3" x:Name="PwdBox"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,32,0,0">
            <Button Content="Entrar" Width="100" Command="{Binding LoginCommand}" Click="Button_Click" Height="73"/>
            <Button Content="Sair" Width="100" Margin="10,0,0,0" Click="Exit_Click" Height="71"/>
            <TextBlock Text="{Binding Status}" Foreground="{StaticResource ErrorColor}" Margin="10,5,0,0"/>
        </StackPanel>
    </Grid>
</Window>
